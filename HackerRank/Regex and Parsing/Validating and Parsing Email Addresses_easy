import email.utils
import re

n = int(input())
pattern = r'^[a-zA-Z][\w\.-]*@[a-zA-Z]+\.[a-zA-Z]{1,3}$'

for _ in range(n):
    s = input()
    name, eml = email.utils.parseaddr(s)
    if re.match(pattern, eml):
        print(email.utils.formataddr((name, eml)))
