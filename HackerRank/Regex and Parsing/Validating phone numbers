import re 
n = int(input())

pattern = r'^[789]\d{0,9}$'
for _ in range(n):
    num = input().strip()
    if len(num) < 10:
        print("NO")
    elif re.match(pattern, num):
        print("YES")
    else:
        print("NO")
    
