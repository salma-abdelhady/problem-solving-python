from collections import Counter

n = int(input())

lst = []
for _ in range(n):
    word = input()
    lst.append(word)
    
unique = len(set(lst))  
freq = Counter(lst)

print(unique)
for i in Counter(freq).values():
    print(i,end=' ')
