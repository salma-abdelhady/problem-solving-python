from collections import Counter

n, k = map(int,input().split())
arr = list(map(int,input().split()))

freq = Counter(arr)
pairs = 0

for i in arr:
    freq[i] -= 1
    pairs += freq[k - i]
        
print(pairs)
