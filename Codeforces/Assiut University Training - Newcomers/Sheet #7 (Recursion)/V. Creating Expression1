n, x = map(int,input().split())
arr = list(map(int,input().split()))
 
def rec(ans=arr[0],i=1):
    if i >= n:
        return ans == x
    
    return rec(ans + arr[i], i+1) or rec(ans - arr[i], i+1)
    
res = rec()
 
if res:
    print("YES")
else:
    print("NO")
